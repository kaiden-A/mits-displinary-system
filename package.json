{
  "name": "mits-spsm",
  "version": "0.1.0",
  "private": true,
  "description": "MITS SPSM — Sistem Pembangunan Sahsiah Murid (Next.js client + FastAPI server)",
  "scripts": {
    "start": "npm run install:server && npm run install:client",
    "install:server": "cd server && uv sync",
    "install:client": "cd client && npm install",
    "dev": "node scripts/dev.js",
    "dev:server": "cd server && uv run server",
    "dev:client": "cd client && npm run dev"
  }
}